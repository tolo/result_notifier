name: result_notifier
description: “Pragmatic state management for Flutter, without the magic”
version: 0.1.0
homepage: https://github.com/tolo/result_notifier

environment:
  sdk: '>=3.2.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  meta: ^1.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
