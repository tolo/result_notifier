name: result_notifier
description: Pragmatic state management for Flutter, based on familiar and platform-native concepts
version: 0.3.0+1
homepage: https://github.com/tolo/result_notifier

environment:
  sdk: '>=3.2.0 <4.0.0'
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter

  meta: ^1.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^2.3.0

flutter:
