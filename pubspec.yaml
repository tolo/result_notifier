name: result_notifier
description: Pragmatic quality-of-life enhancements to vanilla Flutter state management
version: 0.6.0
homepage: https://github.com/tolo/result_notifier

environment:
  sdk: '>=3.2.0 <4.0.0'
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter

  meta: ^1.12.0 # Pinned by the Flutter SDK

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^2.3.0

topics:
  - notifier
  - async
  - state
  - watch
  - reactive
